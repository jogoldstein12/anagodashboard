import TodosPageClient from "./_components/TodosPageClient";

export const metadata = {
  title: "Todos | Mission Control",
  description: "Master task list from TODO.md",
};

export default function TodosPage() {
  return <TodosPageClient />;
}
